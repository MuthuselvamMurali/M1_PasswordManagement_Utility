name: Build Linux

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: MakeFile
      run: make run -C 3_Implementation

© 2022 GitHub, Inc.
Term
